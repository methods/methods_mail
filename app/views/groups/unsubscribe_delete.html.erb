

<%= @email %> has been removed from
<%= @contact[0][:group_name] %>


<br><%= form_tag("/unsubscribe_from?email=#{@email}", method: "post") do %>
		
		<%= hidden_field_tag(:email,  @email) %>
		<%= submit_tag("Back to your subscriptions") %>
		
	<% end %>
